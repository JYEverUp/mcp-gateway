spring:
  application:
    name: mcp-gateway

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    cn.bugstack.ai.mcpgateway: info
